// Definición de la estructura de control if() : La estructura de control if en JavaScript se utiliza para tomar decisiones en tu programa. Permite ejecutar un bloque de código si una condición especificada es verdadera (true) y, opcionalmente, ejecutar un bloque diferente si la condición es falsa (false).

const puntaje = 1001;

if (puntaje == 1000) {
    console.log('Sí es igual');
} else {
    console.log(`el puntaje es ${puntaje}`);
}
