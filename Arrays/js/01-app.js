// Un arreglo es una estructura de datos que contiene información relacionada directamente entre sí, donde cada elemento puede ser del mismo tipo de dato o referencia a objetos relacionados, y se almacenan de manera secuencial, generalmente identificados por índices numéricos.

const numeros = [ 10,20,30 ];

const meses = new Array('Enero','Febrero','Marzo',);


console.log(numeros);
console.log(meses);

// Un arreglo tambien puede contener múktiples tipos de datos
const deTodo = [
    'Hola',10,true,'si',null,{ nombre: 'Joel',trabajo: 'programador' },[ 1,2,3 ]
];

console.log(deTodo);
